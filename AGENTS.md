# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal "hello-world" repository containing only a `README.md`. There are no application services, dependencies, build systems, or test suites to run.

- **No dependencies** to install (no `package.json`, `requirements.txt`, etc.).
- **No services** to start.
- **No lint, test, or build commands** are configured.
- If application code is added in the future, update this section with relevant setup, run, lint, test, and build instructions.
